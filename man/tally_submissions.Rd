% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/submissions.R
\name{tally_submissions}
\alias{tally_submissions}
\title{Import a form's submissions}
\usage{
tally_submissions(
  form_id,
  filter = c("all", "completed", "partial"),
  start_date = NULL,
  end_date = NULL,
  account = NULL
)
}
\arguments{
\item{form_id}{The form's ID, as found in \code{\link[=tally_forms]{tally_forms()}} output or in
the form's URL on tally.so.}

\item{filter}{Which submissions to include: \code{"all"} (the default),
only \code{"completed"}, or only \code{"partial"}.}

\item{start_date, end_date}{Optionally restrict to submissions made on
or after/before these dates. A Date, a date-time, or an ISO 8601
string.}

\item{account}{The name of the Tally account whose key to use, e.g.
\code{"work"} for a key stored in \code{TALLY_API_KEY_WORK}. The default \code{NULL}
uses the account selected with \code{\link[=tally_use_account]{tally_use_account()}}, or failing
that the default account (\code{TALLY_API_KEY}).}
}
\value{
A tibble with one row per submission. The first columns are
\code{submission_id}, \code{submitted_at} and \code{is_completed}; the remaining
columns are the form's questions, in form order, named by their
titles (made unique if titles repeat). Answers are the API's
formatted string representation, \code{NA} where a question wasn't
answered.
}
\description{
Fetches all submissions to a Tally form (handling pagination
automatically) and returns them as a wide tibble: one row per
submission, one column per question.
}
\examples{
\dontrun{
tally_submissions("3xLJ5V")
tally_submissions("3xLJ5V", filter = "completed", start_date = "2026-01-01")
}
}
\seealso{
\code{\link[=tally_forms]{tally_forms()}} to find form IDs.
}
