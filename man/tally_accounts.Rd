% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/accounts.R
\name{tally_accounts}
\alias{tally_accounts}
\alias{tally_use_account}
\title{Work with multiple Tally accounts}
\usage{
tally_accounts()

tally_use_account(account)
}
\arguments{
\item{account}{The account to switch to, e.g. \code{"work"}. Use \code{NULL} or
\code{"default"} to switch back to the default account.}
}
\value{
\code{tally_accounts()} returns a character vector of account
names, \code{"default"} first if \code{TALLY_API_KEY} is set.
\code{tally_use_account()} invisibly returns the name of the previously
active account, so you can restore it later.
}
\description{
If you have several Tally accounts, store each account's API key in its
own environment variable in your \code{.Renviron}: \code{TALLY_API_KEY} for the
default account, and \verb{TALLY_API_KEY_<NAME>} for named accounts (e.g.
\code{TALLY_API_KEY_WORK} for an account called \code{"work"}). Account names are
case-insensitive.
}
\details{
\code{tally_accounts()} lists the accounts that have a key available.
\code{tally_use_account()} switches the active account for the rest of the
session; all tallyr functions then use that account unless given an
explicit \code{account} argument.
}
\examples{
\dontrun{
tally_accounts()

tally_use_account("work")
tally_forms() # uses the "work" account

# one-off call on another account, without switching:
tally_forms(account = "rladies")

# switch back, restoring whatever was active before:
previous <- tally_use_account("rladies")
tally_use_account(previous)
}
}
\seealso{
\code{\link[=tally_api_key]{tally_api_key()}} for how keys are resolved, \code{\link[=tally_sitrep]{tally_sitrep()}}
to see your accounts at a glance.
}
